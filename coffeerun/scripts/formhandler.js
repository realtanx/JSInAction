(function (window) {
    let App = window.App || {};

    function FormHandler() {

    }

    FormHandler.prototype.submitHandler = () => {

    };

    App.FormHandler = FormHandler;
    window.App = App;
})(window);